define(['./module'], function (controllers) {
    'use strict';
    controllers.controller("ContactCtro",["$scope", "$rootScope", function ($scope, $rootScope){
        console.info("Contact")
    }])
});
